import 'package:flutter/material.dart';

class ZoneOne extends StatelessWidget {
  const ZoneOne({super.key});

  @override
  Widget build(BuildContext context) {
    return Text("Zone 1", style: TextStyle(fontSize: 24));
  }
}
